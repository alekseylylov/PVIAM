# Часто встречающиеся проблемы и пути их устранения

| Проблема                                                                                                                                                                                                                       | Причины возникновения проблем                                                                                                                                                            | Пути устранения                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| При ответе от бэкенд HTTP кодом 401, в браузер ответ не доходит, и соединение рвется до отправки заголовков                                                                                                                    | 1. Вackend при ответе с 401 кодом так же добавлял заголовок WWW-Authenticate, что приводило к сбою на промежуточном компоненте, и соединение разрывалось.                                | Не добавлять заголовок WWW-Authenticate на стороне бэкенд.                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Не появляется запрос на ввод второго фактора аутентификации                                                                                                                                                                    | 1. При попытке перейти в раздел защищенный двухфакторной аутентификацией, не открывается форма для ввода второго фактора                                                                 | 1. Разделить cookie `proxy_session_name` (задать для cookie `proxy_session_name` разные имена)                                                                                                                                                                                                                                                                                                                                                                              |
| При авторизации пользователя в сервисе, IAM Proxy не показывает ответ 401                                                                                                                                                      | 1. При авторизации пользователя в сервисе, проверяется статус учетной записи и если status=1, то на api возвращается ответ 401, но IAM Proxy на своей стороне не показывает код ответа   | 1. Отключить HTTP-заголовок `WWW-Authenticate`                                                                                                                                                                                                                                                                                                                                                                                                                              |
| При обращении пользователя передается securityCode, но IAM Proxy перенаправляет на страницу авторизации, вместо возвращения URL входа                                                                                          | 1. При обращении с securityCode, IAM Proxy получает ошибку с кодом 302 и перенаправляет на страницу авторизации, вместо перехода на URL                                                  | 1. Параметры из запросов к ресурсу не попадают в запрос на аутентификацию в сторону провайдера/keycloak                                                                                                                                                                                                                                                                                                                                                                     |
| При нагрузках от 400rps, часть запросов от Istio к IAM завершается с ответом 504. Все запросы с 504 ответом имеют одно и то же время 3000ms. Наблюдается большое время отклика на IAM на некоторых запросах при нагрузках 50%  | 1. При задании `PROXY_KEEPALIVE_BACKEND_CONNECTIONS`, заголовок `Connection` всегда заполняется значением `Close`                                                                        | 1. При TPS 400+ включить `PROXY_KEEPALIVE_BACKEND_CONNECTIONS`, заголовок `Connection` в запросе должен или отсутствовать, или заполняться значением `Keep-Alive`                                                                                                                                                                                                                                                                                                           |
| После обновления на одном из хостов СПО IAM Proxy на последнюю версию, получаем ошибки вида:[401] Error: Error auth by OIDC. You need to authenticate                                                                          | 1. Одновременная успешная работа IAM Proxy на разных версиях не гарантируется                                                                                                            | 1. Убедиться, что на всех хостах установлен IAM Proxy одной версии                                                                                                                                                                                                                                                                                                                                                                                                          |
| На стороне IAM Proxy нет включения `client_id` в строку url                                                                                                                                                                    | 1. В IAM Proxy нет настроек/опций, отвечающих за включение `client_id` в строку url                                                                                                      | 1. Указать в URL следующие `GET` параметры: `revoke`, `logout_idp`, `backchannel`. Описание параметров приведено в разделе [Подключение и конфигурирование](../developer-guide-AUTH/configuration.md). 2. Для отключения фронтового logout установить опцию `oidc_disable_logout_in_idp` в `true`. Описание опции `oidc_disable_logout_in_idp` приведено в разделе [Соответствие имен параметров для разных сред и инструментов установки](../installation-guide/params.md) |
| При redirect IAM Proxy запоминает текущую страницу пользователя, но не запоминает якоря (данные указанные после символа #)                                                                                                     | 1. В IAM Proxy отсутствует функциональность сохранения якорей                                                                                                                            | 1. Якоря не отправляются браузерами в HTTP запросах, и соответственно реализация работы с якорями возможна только на стороне фронтенд (часть приложения работающая в браузере)                                                                                                                                                                                                                                                                                              |
| IAM Proxy не передает параметры запроса при выполнении redirect на Keycloak.SE (KCSE)                                                                                                                                          | 1. В IAM Proxy отсутствует функциональность передачи параметров при redirect                                                                                                             | 1. Заполнить необходимый параметр заново после выполнения redirect                                                                                                                                                                                                                                                                                                                                                                                                          |

## Работы по восстановлению

## В случае сбоев проанализируйте логи на наличие ошибок

### Прокси сервер (IAM Proxy)

Состояние сервиса:

```
systemctl status iamproxy
```

`/opt/iamproxy/logs/access.log`

`/opt/iamproxy/logs/error.log`

При использовании IAM Proxy как программного балансировщика

`/opt/iamproxy/lb-logs/access-lb-iamproxy.log`

`/opt/iamproxy/lb-logs/error-lb-iamproxy.log`

### Клиент по конфигурированию маршрутов (RDS Client)

`/opt/iamproxy/rds-client/logs/log-\*.log`

### Сервис обработки логов (Fluent Bit)

Для просмотра статуса сервиса, необходимо выполнить команду `systemctl status fluent-bit -n 1000` в терминале.

Команда позволяет получить часть журнала по службе systemd Fluent Bit.

## Проверить наличие свободных ресурсов на проблемных серверах (disk/cpu/mem)

Наличие свободных системных ресурсов должно отслеживаться в рамках используемых систем мониторинга.

Вручную сделать это можно так - зайти под системной учетной записью на сервера по SSH, и убедиться что ресурсы
disk/cpu/mem не исчерпаны.

Проверить свободное место на диске, команда: `df -h`.

Проверить свободную память, команда: `vmstat -s`.

Проверить загрузку cpu, команда: `top`.

## Перезапустить сервисы

Для перезапуска используются команды из ssh-консоли:

`sudo systemctl restart iamproxy`

`sudo systemctl restart lb-iamproxy`

`sudo systemctl restart rds-client`

`sudo systemctl restart keycloak`

`sudo systemctl restart fluent-bit`

## Предоставление доступа к логам

Чтобы иметь возможность просмотра логов/конфигурационных файлов без использования sudo, нужно дать доступ для рабочей
системной непривилегированной учетной записи linux (например ivanov-ii). Для этого на серверах необходимо включить
непривилегированную учетную запись в группы сервисов.

Сделать это можно под root-ом такой командой (пример для логина `ivanov-ii`):

```
usermod ivanov-ii -a -G iamproxy
```

```
группа keycloak
usermod ivanov-ii -a -G keycloak

группа iamproxy (сервисы iamproxy, lb-iamproxy, rds-client)
usermod ivanov-ii -a -G iamproxy

```

После этого директории, журналы и большинство файлов, потенциально не содержащих секреты, станут доступны под учетной
записью  `ivanov-ii`.

Также будут права на перезапуск служб ТС, для членов групп сервисов (т.е. под ivanov-ii можно например будет сделать
перезапуск IAM Proxy - `sudo systemctl restart iamproxy`).




