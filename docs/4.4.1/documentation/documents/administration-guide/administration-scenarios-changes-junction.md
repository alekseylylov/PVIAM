# Изменение параметров ответвления (junction)

## Проблематика  
Администраторы сталкиваются с необходимостью обновления параметров существующих ответвлений (junction) IAM Proxy для адаптации к измененным требованиям (например, изменение эндпоинта сервиса, таймаута или политики авторизации). Некорректные изменения могут привести к нарушению работы связанных компонентов или потере доступа к сервисам.

## Предусловия  
1. **Доступ к среде**:  
   - доступ к интерфейсу PACMAN (CFGA) с правами администратора;  
   - знание расположения и структуры YAML-файлов конфигурации IAM Proxy.  

2. **Требуемые навыки**:  
   - опыт работы с PACMAN (CFGA) и редактированием конфигурационных файлов;  
   - понимание параметров настройки `JunctionConfig` и их влияния на работу IAM Proxy (см. [Параметры настройки](config-parameters.md)).  

3. **Инструменты**:  
   - текстовый редактор для редактирования YAML-файлов;  
   - утилиты для проверки синтаксиса YAML (например, `yamllint`).
  
## Правила эксплуатации  
1. **Безопасность конфигураций**:  
   - все изменения в конфигурациях должны проходить через PACMAN (CFGA) или версионный контроль (Git);  
   - используйте TLS для всех API-запросов к IAM Proxy.  

2. **Форматы данных**:  
   - конфигурационные файлы должны соответствовать схеме YAML;  
   - при использовании PACMAN (CFGA):  
     ```yaml
     junctions:
       - name: "example-junction"
         zone: "ZoneA"
         config:
           endpoint: "https://api.mycompany.ru"
           timeout: 30s
     ```

3. **Особенности настройки**:  
   - перед изменением проверьте зависимости ответвления (например, интеграцию с MONA или другими сервисами);  
   - для контуров с высокой нагрузкой рекомендуется тестировать изменения в тестовой среде перед применением в прод.  
  
## Последовательность выполнения  

### Способ 1: Через PACMAN (CFGA)  
1. **Открыть UI PACMAN (CFGA)**:  
   - выберите артефакт `rds-server` для текущего контура.  

2. **Изменить параметры ответвления**:  
   - найдите нужное ответвление в списке `JunctionConfig`;  
   - отредактируйте параметры (например, измените `endpoint` или `timeout`). Пример:  
     ```yaml
     junctions:
       - name: "example-junction"
         zone: "ZoneA"
         config:
           endpoint: "https://api.new-endpoint.com"  # <-- Изменено
           timeout: 45s                            # <-- Изменено
     ```  

3. **Сохранить изменения**:  
   - нажмите "Сохранить" и дождитесь применения настроек.  

### Способ 2: Через YAML-файл  
1. **Редактировать конфигурационный файл**:  
   - откройте YAML-файл, связанный с IAM Proxy;  
   - найдите секцию `JunctionConfig`, содержащую изменяемое ответвление; 
   - обновите параметры. Пример:  
     ```yaml
     junctions:
       - name: "example-junction"
         zone: "ZoneA"
         config:
           endpoint: "https://api.updated-service.com"  # <-- Изменено
           timeout: 60s                                # <-- Изменено
     ```  

2. **Применить изменения**:  
   - сохраните файл и перезагрузите конфигурацию IAM Proxy (через команду `reload` или автоматическую синхронизацию PACMAN).
  
## Результат  
Параметры ответвления (junction) обновлены и интегрированы в архитектуру IAM Proxy. Проверьте:  
- корректность нового эндпоинта через тестовый запрос:  
  ```shell
  curl -k https://iam-proxy.mycompany.ru/example-junction/
  ```  
- логи IAM Proxy на наличие ошибок;
- согласованность изменений в интерфейсе PACMAN (CFGA).

## Описание механизмов безопасности
1. **Ограничение доступа**:
  - доступ к редактированию конфигураций ограничен правами администратора;
  - все API-запросы к IAM Proxy защищены TLS 1.2+/mTLS.

2. **Целостность данных**:
  - YAML-файлы хранятся в зашифрованном виде в SecMan;
  - пример типовой конфигурации после изменений:
    ```yaml
    junctions:
      - name: "secure-junction"
        zone: "SecureZone"
        config:
          endpoint: "https://api.secure-service.com"
          auth:
            type: "mTLS"
            cert_path: "/etc/secrets/client.crt"
            key_path: "/etc/secrets/client.key"
    ```  

3. **Логирование и аудит**:
  - все изменения в конфигурациях фиксируются в журнале PACMAN;
  - активируйте мониторинг изменений через MONA для отслеживания активности.
